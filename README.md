# EVAM_SFN

Swiss French numbers project.

- `index.html`: Swiss French numbers 1-100 in a 10x10 grid (click any cell to hear pronunciation).
- `styles.css`: Shared UI styles.

Ouvrir `index.html` dans votre navigateur pour explorer la grille et activer l'audio (SpeechSynthesis).